name: Run trading bot

on:
  workflow_dispatch:
  schedule:
    # Market open (9:30 AM ET = 13:30 UTC, weekdays only)
    - cron: "30 13 * * 1-5"

    # 1 hour before close (3:00 PM ET = 19:00 UTC, weekdays only)
    - cron: "0 19 * * 1-5"

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
      DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
      LOG_LEVEL: INFO
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run bot
        run: python main.py 